import { SidebarProvider } from '@/context/SidebarContext';

export const metadata = {
  title: 'Weam NextJS',
  description: 'Generated by Weam ',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        <SidebarProvider>
          {children}
        </SidebarProvider>
      </body>
    </html>
  )
}
